name: Security Scan

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Scanner
        run: python scanner.py --path . --output results.json --fail-on-high --policy policy.yaml --policy-env prod --sarif-output results.sarif --no-llm

      - name: Upload JSON Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scan-results
          path: results.json

      - name: Upload SARIF Results
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif